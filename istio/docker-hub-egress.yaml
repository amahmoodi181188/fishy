apiVersion: networking.istio.io/v1alpha3
kind: ServiceEntry
metadata:
  name: allow-docker-hub
  namespace: istio-system
spec:
  hosts:
  - "index.docker.io"
  ports:
  - number: 443
    name: https
    protocol: HTTPS
  - number: 80
    name: http
    protocol: HTTP
  resolution: DNS

